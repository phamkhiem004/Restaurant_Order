// Nest conditionally loads WebSocket and microservice packages. This backend
// does not use either feature, so Workers can replace those optional imports.
export const SocketModule = undefined;
export const MicroservicesModule = undefined;
